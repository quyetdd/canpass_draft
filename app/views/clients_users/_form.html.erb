<%= form_for(@clients_user) do |f| %>
  <% if @clients_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clients_user.errors.count, "error") %> prohibited this clients_user from being saved:</h2>

      <ul>
      <% @clients_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br />
    <%= f.number_field :client_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
