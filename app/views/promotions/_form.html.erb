<%= form_for(@promotion) do |f| %>
  <% if @promotion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@promotion.errors.count, "error") %> prohibited this promotion from being saved:</h2>

      <ul>
      <% @promotion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Promotion name" %><br />
    <%= f.text_field :promotion_name %>
  </div>
  <div class="field">
    <%= f.label "Category" %><br />
    <%= f.collection_select :promotion_category_id, PromotionCategory.all, :id, :category_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
